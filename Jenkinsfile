pipeline {
  agent any
  stages {
    stage('') {
      steps {
        sh 'flutter build linux --release'
      }
    }

  }
  environment {
    PATH = '$PATH:/home/opyrusdev/development/flutter/bin'
  }
}